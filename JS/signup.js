
   document.getElementById("nextBtn").addEventListener("click", function (e) {
      e.preventDefault(); // Prevent form submission
      window.location.href = "../pages/subscription.html"; // Navigate to the subscription page
   });
